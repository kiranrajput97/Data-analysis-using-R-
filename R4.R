
file1<-read.csv(file.choose())
file1

A1<-subset(abc, abc$year of release="2007")
A1


B1<-subset(abc, abc$film="127 hours")
B1

abc.mean(me<-mean(abc$budget..million))
me
maximum<-max(abc$budget..million)
maximum
manimum<-min(abc$budget..million)
minimum

name_of_the_movie3<-subset(abc,abc$flim="Eagle Eye")
name_of_the_movie3

name_of_the_movie4<-subset(abc,abc$genre="comedy" & abc$year of release="2009")
name_of_the_movie4



#====================================

factorail<-function(n)
{
  p=1
  for( i in 1:n){
    p=P*i
    print(p)
  }
}
factorail(10)



#==========================================

list<-list(c(1:20),matrix(nrow=3,ncol=3, byrow=TRUE, C(1,9),"HELLO WORLD")
list
names(list[1])<-("number")
list
names(list[2])<-("matrix")
list
names(list[3])<-("string")
list



#===========================






